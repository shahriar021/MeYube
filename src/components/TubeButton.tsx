import React from "react";

export default function TubeButton({ title }: { title: string }) {
  return (
    <div className=" ">
      <ui>
        <li className=" bg-gray-400">{title}</li>
      </ui>
    </div>
  );
}
